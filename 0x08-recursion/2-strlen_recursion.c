#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen_recursion: length of string
 * @s: string 
 * Return: strlen(s) length of string
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
