#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strcmp - compares strings
 * @s1: first string
 * @s2: second string
 * Return: strcmp results
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
