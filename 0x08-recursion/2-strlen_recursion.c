#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen_recursion - length of string to check
 * @s: string to check length of
 * Return: strlen(s) length of string
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
