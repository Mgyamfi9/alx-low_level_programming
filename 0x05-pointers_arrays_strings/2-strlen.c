#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - checks length of string
 * @s: string to check length
 * Return : strlen the length of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
