#include "main.h"
#include <stdlib.h>
#include <stdio.h>
void *_calloc(unsigned int nmemb, unsigned int size)
{
	if (nnemb == 0 || size == 0)
	      return (NULL);
	ptr = malloc(size * nmemb);
	memset(ptr, 0, (size * nmemb);
	return ptr;	
}
